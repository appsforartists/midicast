{
  "scripts": {
    "bootstrap": "$( yarn bin )/lerna bootstrap --hoist",
    "build": "$( yarn bin )/lerna run build"
  },
  "devDependencies": {
    "lerna": "^2.0.0-rc.5",
    "pundle": "^2.0.0-beta12",
    "pundle-cli": "^2.0.0-beta12",
    "pundle-dev": "^2.0.0-beta12",
    "pundle-preset-default": "^2.0.0-beta12",
    "pundle-preset-typescript": "^2.0.0-beta12",
    "tslint": "^4.2.0",
    "typescript": "2.3.1"
  }
}
